const gridDimensions = 100;
const gridSize = 20;
const plotSize = gridDimensions/gridSize;
const Scale=plotSize/60 //this is only for the current models

const ID=1; ///////VERY TEMPORARY ID CONST, OVO MENJA KOJI GRAD MENJAS

export {gridDimensions,gridSize,plotSize,Scale,ID}